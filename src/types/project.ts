export interface PhotoDetail {
  photo: string;
  title: string;
}

export interface TypePeriod {
  start: Date;
  end: Date;
}

export interface ProjectArea {
  name: string;
  description: string;
  color: string;
  technologies: string[];
}
