export interface ProfileSummary {
  tagline: string;
  description: string;
}

export interface ProfileItem {
  name: string;
  value: string | number;
}

export interface UserStrength {
  icon: string;
  title: string;
  color: string;
  description: string;
}
